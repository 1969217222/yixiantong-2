const BASE_URL ='http://study.jsplusplus.com/Yixiantong/';

const API = {
    // 后端已经写好的接口 只需要定义就行
    GET_HOME_DATAS: BASE_URL + 'getHomeDatas', 
    GET_LIST_DETAS: BASE_URL + 'getListDatas',
    GET_DETAIL: BASE_URL + 'getDetail',
    SEARCH_ACTION: BASE_URL + 'searchAction',
}

export default{ API }